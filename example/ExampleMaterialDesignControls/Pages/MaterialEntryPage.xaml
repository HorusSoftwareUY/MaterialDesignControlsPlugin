<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:material="clr-namespace:Plugin.MaterialDesignControls;assembly=Plugin.MaterialDesignControls"
             x:Class="ExampleMaterialDesignControls.Pages.MaterialEntryPage"
             Title="MaterialEntry">
    <ContentPage.Resources>
        <Style TargetType="material:MaterialEntry">
            <Setter Property="LabelFontSize" Value="{StaticResource LabelFontSize}" />
            <Setter Property="LabelTextColor" Value="{StaticResource LabelTextColor}" />
            <Setter Property="FocusedLabelTextColor" Value="{StaticResource FocusedLabelTextColor}" />
            <Setter Property="TextColor" Value="{StaticResource TextColor}" />
            <Setter Property="FontSize" Value="{StaticResource FontSize}" />
            <Setter Property="BackgroundColor" Value="{StaticResource BackgroundColor}" />
            <Setter Property="BorderColor" Value="{StaticResource BorderColor}" />
            <Setter Property="FocusedBorderColor" Value="{StaticResource FocusedBorderColor}" />
            <Setter Property="AssistiveTextColor" Value="{StaticResource AssistiveTextColor}" />
            <Setter Property="AssistiveFontSize" Value="{StaticResource AssistiveFontSize}" />
            <Setter Property="PlaceholderColor" Value="{StaticResource PlaceholderColor}" />
            <Setter Property="ClearIcon" Value="cancel.png" />
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Padding="16" Spacing="16">
            <material:MaterialEntry FieldName="Name" Type="Filled" LabelText="Name" Placeholder="Enter your name"
                                    IsRequired="true" InvalidMessage="The name is required" />
            <material:MaterialEntry FieldName="Mail" Type="Lined" LabelText="Email" Placeholder="Enter your email" LeadingIcon="email.png" Keyboard="Email" 
                                    RegexValidation="^([\w\.\-]+)@([\w\-]+)((\.(\w){2,3})+)$" InvalidMessage="The Email Address is in an invalid format" />
            <material:MaterialEntry Type="Lined" LabelText="Password" Placeholder="Enter your password" IsPassword="true" ShowPasswordIcon="showPassword.png" />
            <material:MaterialEntry Type="Outlined" LabelText="Confirm Password" Placeholder="Enter your password" IsPassword="true" />
            <material:MaterialEntry Type="Filled" LabelText="Phone" Placeholder="Enter your phone" Keyboard="Telephone" />
            <material:MaterialEntry FieldName="ZipCode" Type="Outlined" LabelText="ZIP code" Placeholder="Enter your ZIP code" Keyboard="Numeric" 
                                    RegexValidation="^[0-9]{5}(?:-[0-9]{4})?$" InvalidMessage="The zip code must have a 5-digit format" />
            <material:MaterialButton Type="Filled" Text="Save" Icon="save.png" Command="{Binding TapCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
