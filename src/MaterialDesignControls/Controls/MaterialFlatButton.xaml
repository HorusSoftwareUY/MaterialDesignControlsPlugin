<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:Plugin.MaterialDesignControls;assembly=Plugin.MaterialDesignControls"
             x:Class="Plugin.MaterialDesignControls.MaterialFlatButton"
             HorizontalOptions="FillAndExpand">
    <ContentView.Content>
        <Frame x:Name="frmContainer" BackgroundColor="Transparent" BorderColor="Transparent" HasShadow="false" MinimumHeightRequest="40" HeightRequest="40" Padding="12,0" HorizontalOptions="FillAndExpand">
            <StackLayout Orientation="Horizontal" Spacing="12" VerticalOptions="Center" HorizontalOptions="Center">
                <Image x:Name="imgIcon" VerticalOptions="Center" HorizontalOptions="End" WidthRequest="24" HeightRequest="24" />
                <controls:MaterialLabel x:Name="lblText" VerticalOptions="Center" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" />
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>
