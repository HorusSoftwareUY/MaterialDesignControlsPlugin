<?xml version="1.0" encoding="UTF-8" ?>
<StackLayout
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Plugin.MaterialDesignControls;assembly=Plugin.MaterialDesignControls"
    x:Class="Plugin.MaterialDesignControls.Implementations.MaterialFieldLayout"
    Spacing="2">
    <controls:MaterialLabel x:Name="lblLabel"
                            LineBreakMode="NoWrap"
                            Margin="14,0,14,2"
                            HorizontalTextAlignment="Start" />
    <Frame x:Name="frmContainer"
           HasShadow="false"
           HeightRequest="40"
           Padding="12,0">
        <StackLayout x:Name="stcContainer"
                     CompressedLayout.IsHeadless="true"
                     Orientation="Horizontal"
                     Spacing="12" />
    </Frame>
    <BoxView x:Name="bxvLine"
             HeightRequest="1"
             HorizontalOptions="FillAndExpand" />
    <controls:MaterialLabel x:Name="lblAssistive"
                            LineBreakMode="NoWrap"
                            Margin="14,2,14,0"
                            HorizontalOptions="FillAndExpand"
                            HorizontalTextAlignment="Start" />
</StackLayout>
